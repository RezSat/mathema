at the end of running the make.sh file inside src:
    free(): invalid pointer

at the end of running the make.sh file inside src sometimes:
     munmap_chunck() : invalid pointer

every time passing the Token over and over again should be fixed, cuz its just repeatedly used so make a functinn
to do it simply.